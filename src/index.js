import ReactDOM from 'react-dom/client'; //imports react. can now use react features

import './index.css';
import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root')); //main hook
root.render(<App />); //this is jsx -> special syntax invetnted by javascript team
